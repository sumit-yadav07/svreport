import express from 'express';

const router = express.Router();

// Authentication is now handled directly with the API
// This file is kept for potential future local authentication needs

export { router as authRoutes };